@page "/edit-professional-area/{ProfessionalAreaId:int}"

@namespace web.Pages

@using web.Services.Session
@using web.Components
@inject NavigationManager NavManager
@inject ISession SessionStorage
@inject Services.Http.ProfessionalArea ProfessionalArea

@using System.Net.Http
@using System.Net.Http.Headers
@using web.Models
@inject HttpClient HttpClient

@inject web.Services.Http.ProfessionalArea ProfessionalAreaService

<main class="container mx-auto">
    <section class="pt-5 d-flex justify-content-between align-items-center mb-5">
        <h2>Edit Professional Area</h2>
        <LogoutButton/>
    </section>

    <form class="crud-edit-form">
        <div class="form-group">
            <label for="area">Area</label>
            <input type="text" class="form-control" id="area" @bind="@professionalArea.Area">
        </div>

        <button type="button" class="btn btn-primary" @onclick="UpdateProfessionalArea">Update</button>
    </form>
</main>

@code {
    private web.Models.ProfessionalArea professionalArea;

    [Parameter]
    public int ProfessionalAreaId { get; set; }

    protected override async Task OnInitializedAsync()
    {
        // professionalArea = await ProfessionalAreaService.GetProfessionalAreaById(ProfessionalAreaId);
    }

    private async Task UpdateProfessionalArea()
    {
        // await ProfessionalAreaService.UpdateProfessionalArea(professionalArea);

        NavManager.NavigateTo("/");
    }
}